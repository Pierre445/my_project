
    

{% extends 'base.html.twig' %}

{% block title %}Hello HeroController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>LISTE TON HEROS 974 ✅</h1>

    <ul>
    {% for hero in listeHeros %}

    <li>{{hero.prenom}}
        {{hero.nom}}
        {{hero.pseudo}}
        {{hero.puissance}}
        <a href="{{path('showOneHero',{'id':loop.index0})}}">Afficher</a>

    {% endfor %}
    </ul>

    <table id="JK">
    <thead>
        <tr><th>Prenom</th>
        <th>Nom</th>
        <th>Pseudo</th>
        <th>Puissance</th></tr>
    </thead>
    
    <tbody>
    {% for Hero in listeHeros %}
        <tr>
             <td>{{Hero.prenom}}</td>
            <td>{{Hero.nom}}</td>
            <td>{{Hero.pseudo}}</td>
            <td>{{Hero.puissance}}</td>
        </tr>
        {% endfor %}
    </tbody>
    </table>
    
</div>
        

{% endblock %}
